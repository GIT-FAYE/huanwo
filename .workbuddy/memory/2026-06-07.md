# 2026-06-07 工作日志

## 唤我（HuanWo）项目前期规划文档

完成了「唤我」小工具合集应用的完整规划文档体系，共 5 份文档：

- docs/README.md：文档索引 + 核心决策摘要
- docs/PRD.md：产品需求文档
- docs/ARCHITECTURE.md：技术架构文档
- docs/DESIGN_SPEC.md：UI/UX 设计规范
- docs/TOOLS_SPEC.md：第一批工具（图片压缩 + 图片转格式）功能规格
- docs/ROADMAP.md：开发路线图（Phase 0–3）

核心决策：Electron 28 + React 18 + TypeScript，sharp 处理图片，electron-updater 自动更新，插件目录扫描注册架构。

---

## 第二轮反馈修订（2026-06-07 第二轮）

根据同事第二轮改进意见，对全部 8 份文档进行了 10 项修订：

**P1 — 文档一致性（6 项）**
1. TOOLS_SPEC 1.4.1：剪贴板触发描述 "面板呼出时" → "工具窗口打开/获得焦点时"
2. TOOLS_SPEC 2.5：转格式工具 UI 补充"覆盖"输出选项（原只有 2 个选项，现统一为 3 个）
3. PRD 1.2：设计理念 "插件化架构，新工具按规范实现后自动注册" → "可持续扩展：架构支持后续插件化，新工具添加便捷"
4. PRD 2.2：manifest.json 引用 → "由工具配置声明（Phase 2 后迁移至 manifest.json）"
5. DESIGN_SPEC 3.1：manifest 的 windowSize 引用 → "由工具配置声明，Phase 2 后迁移至 manifest 的 windowSize 字段"
6. DESIGN_SPEC 3.3：manifest 的 iconBg/iconColor 引用 → "由工具配置定义，Phase 2 后迁移至 manifest"

**P2 — 技术细节（3 项）**
1. TOOLS_SPEC + ARCHITECTURE：补充 SVG 依赖说明（需 librsvg，Windows 默认缺失，v0.1 暂不启用）
2. PHASE0_SPIKE：sharp 打包配置 "files 配置" → 正确方案 "asar.unpack 或 extraResources"
3. LOGGING.md：主进程日志初始化代码补上 `import fs from 'fs'` + `import path from 'path'` + `import dayjs from 'dayjs'`

**P3 — 流程补充（3 项）**
1. PHASE0_SPIKE：验证报告模板补充环境参数（Electron 版本 / Node 版本 / sharp 版本 / Windows 版本）
2. ROADMAP Phase 0 任务清单："搭建本地日志方案" → "初始化本地日志方案（按 LOGGING.md 配置 electron-log）"
3. DESIGN_SPEC 4.2：补充剪贴板检测成功后的 UI 反馈规范（提示文字 + 自动消失动效）

---

## 第三轮反馈修订（2026-06-07 第三轮）

**P1 — 架构一致性（3 项）**
1. ARCHITECTURE 第8节：日志实现阶段 "Phase 1 开始" → "Phase 0 开始"
2. ARCHITECTURE 1.1 对比表：补充"PRD 目标值 <80MB"行，明确 Electron 接近边缘/Tauri 轻松达标
3. PRD 2.2：新增"窗口关闭行为"表格，明确"关闭工具窗口 ≠ 退出应用"，托盘驻留到手动退出为止

**P2 — 细节补漏（3 项）**
1. TOOLS_SPEC 结尾：补充"临时文件管理"，明确写入 os.tmpdir/huanwo-clipboard/，三个清理时机（窗口关闭/应用退出/启动清理残留）
2. PHASE0_SPIKE 第6项：Tauri 对比表数值加"预期值，待 Spike 实测后填入"标注，"胜出方"列改为"❓ 实测后填写"
3. ROADMAP Phase 1：补充"工具处理逻辑单元测试"任务（image-compress + image-convert 核心函数）；同时在剪贴板任务后补充"临时文件清理"子任务

**P3 — 护养性质（2 项）**
1. README.md：追加第三轮更新记录
2. RELEASE_WORKFLOW 2.2：CI 触发条件改为同时支持 v* tag（主要）和 release/v* 分支（验证用），并加说明与 4.3 节步骤对应

---

## 第五轮反馈修订（2026-06-07 第五轮）

**P1 — 逻辑缺陷（2 项）**
1. PRD 2.4 表："见 2.4 快捷键说明" → "见第 4 节设置中心"（死链修正，2.4 是系统集成表本身）
2. TOOLS_SPEC 末尾剪贴板章节：补充**去重逻辑**——onFocus 时对路径类型做路径比对、对二进制类型做内容 hash 比对，相同内容跳过不追加；防止切走再切回同一剪贴板内容重复填入

**P2 — 任务遗漏（2 项）**
1. ROADMAP Phase 1 任务清单：在剪贴板填入任务后补充"剪贴板去重（onFocus 检测到相同图片时跳过，不重复追加）"
2. ARCHITECTURE 第5节目录结构：在 `electron/` 下补充 `__tests__/` 测试目录（含 image-compress.test.ts / image-convert.test.ts）

**P3 — 交互细节（2 项）**
1. PRD 2.1 快捷面板交互表：新增"再次按下快捷键（Toggle）"行——面板已打开时再按 Alt+Space → 关闭面板（与 Raycast 等同类工具一致）
2. ROADMAP v1.0 进入条件：第 7 条补充"单元测试全部通过，核心处理逻辑覆盖率 ≥ 80%"

---

## 第四轮反馈修订（2026-06-07 第四轮）

**P1 — 事实性错误 / 歧义（3 项）**
1. PRD 2.2 节第 81 行：剪贴板引用 "详见 2.5" → "详见 2.3"（2.5 是自动更新，剪贴板在 2.3）
2. ARCHITECTURE 1.1 对比表："实测约 80–100MB" → "调研基准值 ~80–100MB（待 Spike 实测）"（Spike 还未跑，不应用"实测"措辞）
3. PRD 2.3 + TOOLS_SPEC 1.4.1 + 剪贴板作为输入源章节：明确剪贴板填入为追加模式，不覆盖已选文件，并在 TOOLS_SPEC 加具体场景说明

**P2 — 定义缺失（2 项）**
1. ROADMAP Phase 1 单元测试任务：补充最小覆盖范围（格式转换正确性 JPEG→WebP/PNG→WebP/GIF→WebP、尺寸缩放计算、边界条件 0 字节/超大文件/不支持格式、压缩后反增保留逻辑）
2. PRD 设置中心快捷键：补完整冲突处理流程（冲突 → 托盘弹气泡提示 → 设置页标记 → 引导自定义；冲突时快捷键失效但应用仍可用）

**P3 — 轻量建议（2 项）**
1. PHASE0_SPIKE 第 3 项验证步骤：.ico 三文件方案 → 单文件含 16/32/64/256 四种尺寸方案
2. PRD 2.4 系统集成表：新增"前置依赖"列（开机自启依赖注册表、右键菜单依赖 Shell Extension 需管理员权限、全局快捷键依赖 nativeTheme 等）

---

## 第六轮反馈修订（2026-06-07 第六轮）

**#1 功能遗漏 — 缺少处理取消/中断机制（1 项）**
1. TOOLS_SPEC：工具1（1.5 UI 布局）补充处理中操作栏变化（"开始压缩"→"中止处理"）；新增 1.7 中止处理章节（完成当前文件即停止，已处理结果保留）
2. TOOLS_SPEC：工具2（2.5 UI 布局）同步补充处理中"中止处理"按钮；2.6 边界条件补充中止逻辑；新增 2.7 中止处理（引用 1.7）
3. DESIGN_SPEC 4.3 执行状态表：新增"中止中"和"完成（已中止）"两行状态定义，补充中止交互细节

**#2 建议 — 工具窗口最小化后恢复方式未定义**
1. PRD 2.2：在"窗口关闭行为"后新增"窗口最小化恢复方式"章节，定义三种恢复方式（Alt+Tab / 面板重开 / 托盘菜单），并说明 Phase 0-1 行为

**#3 建议 — GIF 压缩入口的提示时机**
1. TOOLS_SPEC 1.2：补充 GIF 提示的具体形式（参数区黄色提示条 + 文件列表 🎞️ GIF 标签 + tooltip）
2. DESIGN_SPEC：新增 4.2.1 GIF 输入提示规范章节，定义提示条样式、文件列表标记、触发时机

---
## 第六轮反馈收尾修复（2026-06-07 第六轮收尾）

同事第六轮反馈最终复查，发现 1 项遗留拼写错误：

**P3 — 拼写错误（1 项）**
1. DESIGN_SPEC 4.2.1 第223行：颜色代码 `#FAEDA` → `#FAEEDA`（Warning Light 背景色少写一个 E）

至此，6 轮同事反馈共 **49 项问题** 全部修复完毕，8 份文档均已就绪，可进入 Phase 0 Spike 验证阶段。
